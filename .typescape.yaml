schema_version: 1
manifest:
  name: blackmatter-android
  kind: BlackmatterComponent
  archetype: blackmatter-component-hm-nixos
  description: Android SDK, Emulator, platform tools, and device management.
dimensions:
  vocabulary: [android, sdk, adb, emulator, udev]
  domains: [Nix]
  stack: [HomeManager, NixOS]
  modules:
    home_manager: module/home-manager/
    nixos: module/nixos/
  render:
    produces:
      - "Android SDK environment vars"
      - "NixOS udev rules for ADB"
  compliance:
    satisfies: []
    participates: []
  dag:
    inputs: [nixpkgs, substrate]
  attestation: { merkle_root: null, signed_by: null, producer: blackmatter-component-flake.nix }
